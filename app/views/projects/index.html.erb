<div class="border-left-line">
  <%= t('.main_title') %>
</div>

<div class="main-content">
  <table class="table">
    <tbody>
      <% @projects.each do |project| %>
        <tr><td>
          <%= link_to project.project_name_with_status, '#', class: "text-dark" %>
        </tr></td>
      <% end %>
    </tbody>
  </table>
</div>
